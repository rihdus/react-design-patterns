const path = require("path");

module.exports = (baseConfig, env, defaultConfig) => {
  // Extend defaultConfig as you need.
  
  // defaultConfig.output.publicPath = 'stories';

  return defaultConfig;
};